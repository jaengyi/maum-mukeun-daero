org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8 -XX:+HeapDumpOnOutOfMemoryError
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

android.useAndroidX=true
android.nonTransitiveRClass=true

# AGP 9 built-in Kotlin과 KSP 호환 — KSP가 generated sources를 kotlin.sourceSets DSL로
# 등록하는데, AGP 9의 built-in Kotlin이 이걸 막는다. workaround로 허용.
# https://developer.android.com/r/tools/built-in-kotlin
android.disallowKotlinSourceSets=false

kotlin.code.style=official
